<VirtualHost *:80>
  ServerAdmin webmaster@{{ DOMAIN }}
  ServerName www.{{ DOMAIN }}
  DocumentRoot {{ DOC_ROOT }}

  <Directory {{ DOC_ROOT }}>
    AllowOverride All
    Order allow,deny
    Allow From All
  </Directory>

  CustomLog /var/log/apache2/{{ SDOMAIN }}.access.log combined
  ErrorLog /var/log/apache2/{{ SDOMAIN }}.error.log
</VirtualHost>
